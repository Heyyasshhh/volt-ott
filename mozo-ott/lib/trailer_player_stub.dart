// trailer_player_stub.dart
// This file acts as a placeholder for non-web platforms.
// If needed, you can add dummy implementations here.
import 'package:flutter/material.dart';

class TrailerVideoPlayerWeb extends StatelessWidget {
  final dynamic baseItem;

  const TrailerVideoPlayerWeb(this.baseItem, {super.key});

  @override
  Widget build(BuildContext context) {
    // Return an empty Container or a placeholder widget
    return Container();
  }
}
